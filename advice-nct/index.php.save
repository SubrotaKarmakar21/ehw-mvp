<?php

require_once("../../includes-nct/config-nct.php");

$module = "advice-nct";

$reqData = $_REQUEST;

// ✅ load class (IMPORTANT)
require_once("class.advice-nct.php");

// ✅ create object
$obj = new Advice($module, $reqData);

// ✅ page title (like billing)
$winTitle = $headTitle = 'AI Advice - '.SITE_NM;

// ✅ get content
$pageContent = $obj->getPageContent();

// ✅ THIS LINE INJECTS INTO FULL LAYOUT (CRITICAL)
require_once(DIR_TMPL."parsing-nct.tpl.php");
?>1~<?php

require_once("../../includes-nct/config-nct.php");

$module = "advice-nct";

$reqData = $_REQUEST;

// ✅ load class (IMPORTANT)
require_once("class.advice-nct.php");

// ✅ create object
$obj = new Advice($module, $reqData);

// ✅ page title (like billing)
$winTitle = $headTitle = 'AI Advice - '.SITE_NM;

// ✅ get content
$pageContent = $obj->getPageContent();

// ✅ THIS LINE INJECTS INTO FULL LAYOUT (CRITICAL)
require_once(DIR_TMPL."parsing-nct.tpl.php");
?>
